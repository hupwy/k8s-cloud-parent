server:
  port: 8089

management:
#  server:
#    port: 8081
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: k8s-srv-docker-monitor
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    kubernetes:
      loadbalancer:
        mode: service
      discovery:
        enabled: true
      client:
        namespace: hup-ns