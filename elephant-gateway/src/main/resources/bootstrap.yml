spring:
  cloud:
    kubernetes:
      config:
        sources:
          - name: gateway
            namespace: ekephant
      reload:
        enabled: true
        mode: polling
        monitoring-secrets: true
