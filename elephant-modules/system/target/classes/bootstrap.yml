spring:
  cloud:
    kubernetes:
      config:
        sources:
          - name: system
            namespace: cloudlab
      reload:
        enabled: true
        mode: polling
        monitoring-secrets: true
